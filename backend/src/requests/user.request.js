module.exports = {
  rules() {
    // Định nghĩa rules
  },
  messages() {
    // Thông báo nếu lỗi
  },
  validate(body) {
    // Nhận body từ controller
    // Check rules
    // Trả về message nếu lỗi, hoặc trả về body nếu thành công
  },
};
